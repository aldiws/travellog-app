<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Document</title>
</head>

<body>
  <form action="/destination/edit/<%= row.id %>" method="post" enctype="multipart/form-data">
    <input type='file' name="photo" value="<%= row.photo %>" placeholder="photo" accept=".jpg, .jpeg, .png" multiple /> <br>
    <input type="text" name="name" value="<%= row.name %>" placeholder="name"><br>
    <input type="text" name="category" value="<%= row.category %>" placeholder="category"><br>
    <input type="text" name="country" value="<%= row.country %>" placeholder="country"><br>
    <input type="text" name="province" value="<%= row.province %>" placeholder="province"><br>
    <input type="text" name="specificLocation" value="<%= row.specificLocation %>" placeholder="specific location"><br>

    <textarea name="description" placeholder="description"><%= row.description %></textarea><br>
    <input type="submit" value="submit">
  </form>

</body>
</html>